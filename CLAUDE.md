# LumaForge Worktree Preference

## Git worktree policy

For this project, the preferred worktree directory is:

- `.worktrees`

Use `.worktrees/<branch-name>` for new feature worktrees instead of external paths.

