{
  "name": "@lumaforge/luma-raw-runtime",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Performance-first browser RAW runtime for LumaForge.",
  "sideEffects": [
    "./dist/*.wasm",
    "./dist/*.worker.js"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build --config vite.config.ts && tsc -p tsconfig.build.json",
    "build:native": "bash -lc 'if [ -f native/build-libraw.sh ]; then bash native/build-libraw.sh; else echo \"native/build-libraw.sh is not available yet; add it in Task 5.\" >&2; exit 1; fi'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src worker",
    "bench:serve": "vite --host 0.0.0.0 --port 4174 --config vite.config.ts"
  },
  "devDependencies": {
    "libraw-wasm": "^1.1.2"
  }
}
